experiment_name: nyc_lstm_baseline
processed_data_dir: data/processed/nyc_yellow_taxi_2018_manhattan_pickups_15min

model:
  name: lstm
  hidden_dim: 128
  num_layers: 1
  dropout: 0.0

trainer:
  batch_size: 64
  max_epochs: 10
  early_stopping_patience: 3
  gradient_clip_norm: 1.0

artifacts:
  report_dir: artifacts/reports/baselines
  checkpoint_dir: artifacts/checkpoints/baselines
